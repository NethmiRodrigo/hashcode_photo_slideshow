const createSildeShow = tagContainer => {
  const iterator = tagContainer.keys();
  let firstKey = iterator.next().value;
  let firstImages = tagContainer.get(firstKey);

  const slideShow = new Array();
};

module.exports = createSildeShow;
